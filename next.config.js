/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'localhost',
            'rumahjualdesign-production.up.railway.app↗'
        ],
    },
}

module.exports = nextConfig
